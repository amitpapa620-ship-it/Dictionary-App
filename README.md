# 📖 Dictionary App

A sleek, functional web application that allows users to look up word definitions, synonyms, and pronunciations. This project features a custom-built UI with a built-in theme switcher for light and dark modes.

---

## 🚀 Features
* **Real-time Search:** Get instant definitions for any word.
* **Dynamic Themes:** Seamlessly switch between Day Mode and Night Mode (persisted via `theme.js`).
* **Responsive Design:** Optimized for both desktop and mobile viewing.
* **Backend Integration:** Powered by Node.js and Express using EJS templates.
* **Real-time Search: Lightning-fast word lookups using the Free Dictionary API (https://dictionaryapi.dev/).

## 🛠️ Built With
* **Frontend:** EJS (Embedded JavaScript templates), CSS3, Vanilla JavaScript
* **Backend:** Node.js, Express
* **Assets:** Custom background imagery for light/dark modes

## 📦 Installation & Setup

1. **Clone the repository:**
   ```bash
   git clone [https://github.com/YOUR_USERNAME/dictionary-app.git](https://github.com/YOUR_USERNAME/dictionary-app.git)
Install dependencies:

Bash

npm install
Run the application:

Bash

node index.js
Open in browser: Navigate to http://localhost:3000 (or your configured port).

📂 Project Structure
/public - Contains static assets like images, CSS, and client-side JS.

/views - Contains the index.ejs file for the UI layout.

index.js - The main entry point for the Express server.
